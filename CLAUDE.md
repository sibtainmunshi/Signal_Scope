# Continuing SignalScope

Read `docs/AGENT_HANDOFF.md` first, then the PDF/plan/checklist paths it identifies.
This is an existing approved hackathon build; preserve working code, checkpoints,
source attribution and evaluation splits. Check actual running jobs and newer
reports before acting on the handoff snapshot. Use the current `signal_scope`
folder and existing environment. Keep one writer per working tree and one GPU
training job at a time. Report measured outcomes and failures honestly.

After each meaningful milestone, update `docs/PROGRESS.md`,
`docs/SUBMISSION_CHECKLIST.md` and `docs/AGENT_HANDOFF.md` so any assistant can
continue from repository state without relying on another chat's memory.
